# Memory - 2026-03-05

## UCM6308 API Integration - Troubleshooting Progress

### What Works
- Network connectivity to 192.168.1.148 confirmed (ping works)
- HTTP on port 80 reachable and returns HTML
- Basic Auth with "Brigain:Jen!fer1" gives HTTP 200 OK

### Problem Identified
**API endpoint returns web UI instead of JSON data:**
- Endpoint: `http://192.168.1.148/rest/sipaccount/list`
- Expected: JSON array of SIP accounts
- Got: 27KB HTML page (UCM login/web interface)
- Content-Type: `text/html` (not `application/json`)

### Root Cause
**Credential mismatch:**
- "Brigain" is likely a **SIP extension display name**, NOT the UCM6308 web admin username
- The API is rejecting/ignoring our Basic Auth and serving the default public page instead of protected API data
- Need to find the actual admin credentials for UCM6308 web interface

### Next Steps Required (awaiting user input)
1. Find actual UCM6308 admin username/password (not SIP extension creds)
2. User needs to open `http://192.168.1.148` in browser on MSI and check what login it prompts
3. Once correct credentials found, retest API endpoints:
   - `GET /rest/sipaccount/list`
   - `GET /rest/app/hitbruteforcerule/1`
   - `GET /rest/record/list` (CDR)

### Scripts Created
- `UCM-FRESH.ps1` - Basic connectivity test
- `UCM-BASIC.ps1` - Port scanning
- `UCM-DIAGNOSE.ps1` - Full diagnostic showing HTML vs JSON detection
- All saved in workspace for reuse once credentials are correct

### Memory.md Update Needed
After credentials found, update MEMORY.md:
- UCM6308 admin username (actual)
- UCM6308 API authentication method confirmed working
